[project]
name = "gpt-image-2-mcp"
version = "0.1.0"
description = "Local MCP server and CLI for generating ChatGPT images through a browser session."
requires-python = ">=3.11"
dependencies = [
  "mcp>=1.12.4",
  "patchright>=1.48.0",
]

[tool.uv]
package = false
